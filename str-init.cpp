#include<iostream>
using  namespace std;
int main ()
{
    string init("this is cse acl lab");
    string add("now we are in the lab");
    init.append(add);
    cout<<init<<endl;
    return 0;
}

